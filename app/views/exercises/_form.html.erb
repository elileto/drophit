<%= form_with(model: exercise, local: true) do |form| %>
  <% if exercise.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(exercise.errors.count, "error") %> prohibited this exercise from being saved:</h2>

      <ul>
      <% exercise.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :exercise_name %>
  </div>

  <div class="field">
    <%= form.label :intensityLevel %>
    <%= form.number_field :intensityLevel, id: :exercise_intensityLevel %>
  </div>

  <div class="field">
    <%= form.label :sets %>
    <%= form.number_field :sets, id: :exercise_sets %>
  </div>

  <div class="field">
    <%= form.label :hours %>
    <%= form.number_field :hours, id: :exercise_hour %>
  </div>
  <div class="field">
    <%= form.label :minutes %>
    <%= form.number_field :minutes, id: :exercise_minutes %>
  </div>
  <div class="field">
    <%= form.label :seconds %>
    <%= form.number_field :seconds, id: :exercise_seconds %>
  </div>

  <div class="field">
    <%= form.label :weight %>
    <%= form.number_field :weight, id: :exercise_weight %>
  </div>

  <div class="field">
    <%= form.label :repetitions %>
    <%= form.number_field :repetitions, id: :exercise_repetitions %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
